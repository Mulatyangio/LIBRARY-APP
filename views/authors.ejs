<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>library app</title>
    <link rel="stylesheet" href="/css/new.css">
    <link rel="stylesheet" href="/css/authors.css">
</head>
<body>
    <%- include('header.ejs')%>
    <main>
        <section id="hero">
            <h1>Authors</h1>

            <div class="authors">
                
                <% if(locals.authors){%>
                 <ul>
                     <% locals.authors.forEach(auth=>{%>
                      <li>
                           <h2><%= auth.FullName%></h2>
                           <p>Nationality:<%= auth.Nationality%></p>
                          <p>YOB:<%= auth.YOB%></p>
                          <p>Biography</p>
                          <p><%= auth.Biography.toUpperCase()%></p>
                     </li>
                     <% })%>
                   </ul>
             <% }else{%>
                 <h2>Currently there are no authors!!</h2>
              <%}%>
     
             </div>
                 <!--optional chaining -->
            <%if(locals?.user?.Email=="johnmwanda@gmail.com"){%>     
            <form action="/newauthor" method="post">
                <label for="id">ID</label>
                <input type="number" name="id" id="id" placeholder="Enter ID">
                <label for="name">Name</label>
                <input type="text" name="name" id="name">
                <label for="YOB">YOB</label>
                
                <select name="YOB" id="YOB">
                    <%for(let year=2024; year>1970; year--){%>
                        <option value="<%=year%>"><%=year%></option>
                        <%}%>
                </select>
                <label for="nationality">Nationality</label>
                <input type="text" name="nationality" id="nationality">
                <label for="bio">Biography</label>
                <textarea name="bio" id="bio"></textarea>
                <input type="submit" value="Add Author">
            </form>
            <%}%>
        </section>
    </main>
    <footer>
        <p>LibApp &copy;<% new Date().getFullYear()%>.All rights Reserved</p>
    </footer>
</body>
</html>